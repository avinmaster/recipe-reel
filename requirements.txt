# Core API + pipeline (CPU-friendly, always installed). The full pipeline runs in
# MOCK mode with only these. Media/GPU extras live in requirements-gpu.txt.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
sse-starlette==2.2.1
openai==1.59.6          # OpenAI-compatible client → Fireworks AI
httpx==0.28.1
python-dotenv==1.0.1
yt-dlp==2025.6.30       # video/URL ingest + metadata + captions (CPU, light)
